{% extends 'back/base_back.html.twig' %}

{% block title %} - Edition de "{{recipe.name}}"{% endblock %}

{% block content %}
    
	<h1 class="my-5">Edition de
		"{{recipe.name}}"</h1>
	<a href="{{ path('app_back_movie_index') }}">Retour à la liste</a>

	
	{{ include('back/movie/_form.html.twig', {'button_label': 'Update'}) }}
	{{ include('back/movie/_delete_form.html.twig') }}
					
{% endblock %}
